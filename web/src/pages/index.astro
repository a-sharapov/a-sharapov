---
import Canvas from '@lib/components/Canvas/Canvas.svelte'
import Vignette from '@lib/components/Vignette/Vignette.svelte'
import Base from '@lib/layouts/Base.astro'

const locale = 'ru'
---

<Base locale={locale}>
  <Canvas client:only="svelte" slot="assets" transition:persist />
  <Vignette slot="assets" transition:persist />
</Base>

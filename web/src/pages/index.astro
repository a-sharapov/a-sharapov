---
import Canvas from '@lib/components/Canvas/Canvas.svelte'
import Base from '@lib/layouts/Base.astro'

const locale = 'ru'
---

<Base locale={locale}>
  <Canvas client:only="svelte" slot="assets" transition:name="Scene" transition:persist />
</Base>
